# GraspDataGen TODO

This file tracks the project roadmap for Codex/agent work. Keep it current when gripper adaptation status changes, a new candidate is discovered, or a failure mode is diagnosed.

## Core Goals

- Adapt multiple two-finger grippers so they can generate correct grasp data that passes real IsaacLab/PhysX validation.
- Ensure each adapted gripper has a usable pure gripper USD in `bots/`, an entry in `GRIPPER_CONFIGS`, a regenerated gripper definition, plausible guess output, and successful simulation-validated grasp YAML.
- Upgrade `grasp_sim.py` validation from a single acceptance check into staged validation:
  1. normal grasp attempt and hold;
  2. hold under multiple random-direction disturbance forces;
  3. after the gripper has already grasped the object, invert the gripper and object together, then apply gravity to the object to test whether the grasp remains stable.
- Treat only grasps that pass all three validation stages as final successful grasp data.
- Identify strong default grasp generation and simulation settings for each gripper instead of assuming one gripper's parameters transfer to another.

## Agent Operating Notes

- Treat `grasp_guess.py` output as provisional until `grasp_sim.py` accepts it.
- IsaacLab/PhysX is expected to be available; run focused validation after behavior changes.
- If a full robot or arm USD contains the desired gripper, extract or generate a pure gripper USD and place it in `bots/` before registering it.
- Do not mark a gripper as adapted until actual simulation-validated grasp data exists.
- For `plate.obj` datagen, run the plate-specific validation with `--force_magnitude 0.5` instead of the default disturbance force.
- When repeated runs produce no successful grasps, classify the root cause before tuning further: gripper physical feasibility, USD physics setup, definition extraction, guess generation, or simulation validation.

## Active Gripper Configs

These entries are registered in `scripts/graspgen/gripper_configurations.py`. Initial status is conservative unless verified in the current checkout.

| gripper_config | Source USD | USD physics check | Definition check | Guess check | Sim validation | Current issue | Next step |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `robotiq_2f_85` | `bots/robotiq_2f_85.usd` | Needs full USD physics review | Regenerated `bots/robotiq_2f_85.npz` on 2026-05-09; open state is row 0, `open_limit=lower`, open widths decrease toward closed | Example-set guess smoke produced candidates for banana, mug, caltrop, threelayer, and Sphere | Passed staged sim smoke across `objects/datagen_example.json`: banana 13/16, mug 14/16, caltrop 15/16, threelayer 12/16, Sphere 15/16 final successes; all reported successes passed all three stages | Small/medium sample success exists; USD physics review still incomplete | Inspect USD physics details and run a larger validation batch before treating as a fully tuned baseline |
| `onrobot_rg6` | `bots/onrobot_rg6.usd` | Inspected USD hierarchy and physics on 2026-05-09: rigid bodies, masses, collision APIs, revolute joints, and angular drive present | Regenerated `bots/onrobot_rg6.npz` on 2026-05-09; `open_limit=lower`, row 0 is widest open width `0.1514`, rows close toward `-0.0002` | Banana 0.75 guess smoke produced 1024/1024 provisional candidates; pregrasp row 0 moves toward closed row -1 | Passed staged sim smoke on banana 0.75: 3/16 final successes passed `normal_hold`, both disturbance attempts, and `inverted_gravity_hold` | Small-sample success exists; defaults still need broader object-set tuning | Run larger `datagen.py` across `objects/datagen_example.json` and tune defaults if success rates are weak |
| `franka_panda` | `bots/franka_panda.usd` | Rechecked USD on 2026-05-10: pure gripper hierarchy with prismatic finger joints; final setup uses `panda_finger_joint1` as the only active linear drive and `panda_finger_joint2` as a PhysX mimic follower; root/finger masses still author `physics:mass=0.0`, but split tests showed mass-only repair did not restore success | Regenerated `bots/franka_panda.npz` on 2026-05-10 after switching to active-drive + mimic-follower setup; `open_limit=upper`, row 0 is widest open width `0.0798`, rows close toward `-0.00017`; `driven_joints` is back to only `panda_finger_joint1` | Plate guess smoke produced 32/32 provisional candidates; common-object smoke produced 32/32 provisional candidates for banana, caltrop, mug, and threelayer | Active-drive + mimic-follower temporary USD restored plate force-0 validation from 0/32 to 16/16; repaired repo USD passed plate force-0 16/16; earlier two-drive smoke passed plate force-0 29/32, plate force-1 1/32, banana 26/32, caltrop 20/32, mug 23/32, and threelayer 25/32 | Root cause for broad 0-success runs was Panda master drive setup: `panda_finger_joint1` had zero stiffness/damping, so the mimic follower had no driven motion to follow during `grasp_sim.py` validation | Keep reduced-force handling for shallow plate if needed; run a larger full-object validation with the final active-drive + mimic-follower USD and optionally repair authored zero masses as cleanup |
| `piper_v2_gripper` | `bots/piper_v2_gripper.usd` | Current `bots` USD has small symmetric finger collisions (`link7/link8` collision bboxes about 26 x 56 x 76.5 mm) and active gripper drive | Existing 2026-05-10 data indicates a valid definition/stroke; current checkout may need `bots/piper_v2_gripper.npz` regenerated if absent | Existing full datagen outputs contain saved successes for banana, mug, and handwheel | Existing `datagen_sim_data/piper_v2_gripper` saved successes: banana 681, mug 845, handwheel 206; `plate.yaml` is empty and should be treated as a plate-specific case, not broad failure | No current evidence of the Piper H/L bad finger-collision failure in `bots/piper_v2_gripper.usd` | Regenerate the npz in the current checkout if missing, keep plate on reduced disturbance force, and use V2 as the small-collider reference for Piper repairs |
| `piper_h_v1_gripper` | `bots/piper_h_v1_gripper.usd` | Rechecked on 2026-05-10: current `bots` USD has small symmetric `Link7/Link8` finger collisions, but source `piper_isaac_sim/USD/piper_h_v1.usd` and H URDF still have an oversized `Link8` collision from `link8.dae` | Current checkout had no `bots/piper_h_v1_gripper.npz` during this pass; old zero-width notes are stale. Existing saved datagen outputs show the repaired `bots` asset can work | Existing outputs contain saved successes for banana, mug, and handwheel | Existing `datagen_sim_data/piper_h_v1_gripper` saved successes: banana 739, mug 805, handwheel 230; `plate.yaml` is empty and should be rerun with reduced force | Source/extraction branch is still hazardous: re-extracting from the original H source can reintroduce the oversized `Link8` collider and bad `bite_point`; current `bots` USD appears already repaired | Add an extraction-time bbox sanity check/override, preserve the repaired small finger colliders, regenerate `bots/piper_h_v1_gripper.npz` if missing, and rerun full validation |
| `piper_l_v1_gripper` | `bots/piper_l_v1_gripper.usd` | Rechecked on 2026-05-10: current `bots` USD has the same class of bad source collision as the old H diagnosis; `Link7/collisions` is small, but `Link8/collisions` is oversized (about 268 x 42 x 93.5 mm) | Regenerated `bots/piper_l_v1_gripper.npz` on 2026-05-10; stroke is valid (`open_limit=upper`, widths about `0.1000` to `0.0`), but `finger_indices=[2,1]` makes the bad `Link8` the bite finger and produces `bite_point` about `[0.10598, 0.0, 0.00196]` | Non-plate datagen generated 1024 provisional guesses per object | 2026-05-10 force-1.0 non-plate validation: banana 12/1024, mug 3/1024, handwheel 43/1024. Plate with `--force_magnitude 0.5`: 149/1024 | Blocking branch: bad finger collision geometry and derived bite point, not zero-width stroke. `extract_piper_gripper_usd.py` is copying the source collider rather than creating the geometry error | Replace/crop L `Link8` collision to a small finger-pad collider, regenerate `bots/piper_l_v1_gripper.npz`, and rerun the same probe before tuning grasp parameters |
| `piper_v1_gripper` | `bots/piper_v1_gripper.usd` | Rechecked on 2026-05-10: current `bots` USD has small `link7/link8` finger collisions; no evidence of the H/L oversized bite-finger collision problem | Regenerated `bots/piper_v1_gripper.npz` on 2026-05-10; valid stroke (`open_limit=upper`, widths about `0.0994` to `-0.00064`), `open_axis=0`, normal `bite_point` about `[0.00032, -0.00099, -0.02000]` | Non-plate datagen generated 1024 provisional guesses per object, though some objects required multiple guess tries | 2026-05-10 force-1.0 non-plate validation: banana 698/1024, mug 850/1024, handwheel 208/1024. Plate with `--force_magnitude 0.5`: 5/1024 | Not a Piper H-style collision failure. Main weakness is object-dependent, especially handwheel and plate, likely grasp pose/centering/plate geometry rather than broken finger collider extraction | Tune V1 bite/centering/offset strategy and inspect plate/handwheel failed poses; do not classify it with the H/L source collision bug |
| `piper_x_v1_gripper` | `bots/piper_x_v1_gripper.usd` | Rechecked on 2026-05-10: source/current USD still has an oversized `Link8/collisions` bbox, but `Link7/collisions` is small and current extraction does not use the bad side as the bite finger | Regenerated `bots/piper_x_v1_gripper.npz` on 2026-05-10; valid stroke (`open_limit=upper`, widths about `0.1000` to `0.0`), `finger_indices=[1,2]`, normal `bite_point` about `[0.0, 0.0, -0.01503]` | Non-plate datagen generated 1024 provisional guesses per object | 2026-05-10 force-1.0 non-plate validation: banana 953/1024, mug 953/1024, handwheel 788/1024. Plate with `--force_magnitude 0.5`: 885/1024 | Latent USD collision asymmetry exists, but current generated definition and datagen success are high; not currently failing like H/L | Still repair or sanity-check the oversized `Link8` collider to avoid future extraction-order regressions, but treat current X as usable after broader validation |
| `changingtek_ag2f120s` | `bots/changingtek_ag2f120s.usd` | Flattened pure candidate from `robot_usds/grippers/ChangingTek_AG2F120S/`; full per-prim physics audit still needed | Regenerated `bots/changingtek_ag2f120s.npz` on 2026-05-10 during full validation; finite, `open_limit=upper`, row 0 width `0.1021`, closes toward `-0.0012` | Full datagen generated provisional candidates for the current example set and plate | 2026-05-10 helper validation with headed `grasp_sim.py`: banana 463 -> 440, mug 808 -> 768, handwheel 278 -> 246, plate force-0.5 datagen then force-0.6 revalidation 715 -> 605 | Needs review: headed revalidation retained most generated successes, but final rates vs 1024 are low for banana 43.0%, handwheel 24.0%, and plate 59.1%; mug passed at 75.0% | Audit USD physics, then inspect low-success banana/handwheel/plate poses for object-dependent grasp guess coverage, centering, bite depth, and plate disturbance sensitivity before declaring support |
| `changingtek_ag2f90` | `bots/changingtek_ag2f90.usd` | Flattened pure candidate from `robot_usds/grippers/ChangingTek_AG2F90/`; full per-prim physics audit still needed | Regenerated `bots/changingtek_ag2f90.npz` on 2026-05-09; finite, `open_limit=upper`, row 0 width `0.1069`, closes toward `0.0104` | Banana produced 512/512 candidates; mug produced 174 candidates in 3 tries | Staged smoke passed banana 8/16 and mug 3/16 final successes; mug had 11 `normal_hold` and 2 `disturbance_hold` failures | Real success exists but mug success rate is weak | Inspect mug failed poses/contact paths, then tune bite/centering and defaults |
| `galaxea_g1` | `bots/galaxea_g1.usd` | Flattened pure candidate from `robot_usds/grippers/Galaxea_G1/`; quick USD read on 2026-05-10 found rigid base/finger masses, convexHull finger collisions, active `gripper_joint`, and `gripper_joint2` authored as a PhysX mimic follower of `gripper_joint` | Regenerated `bots/galaxea_g1.npz` on 2026-05-10 during helper validation; finite, `open_limit=upper`, row 0 width `0.1174`, closes toward `0.0174`, but extracted `approach_axis=0`, `open_axis=1` and headed visualization shows the gripper lying sideways instead of vertically like the working grippers | Full helper datagen generated 1024 provisional candidates before sim filtering for banana, mug, handwheel, and plate | 2026-05-10 helper validation failed with headed `grasp_sim.py`: banana 0 -> 0, mug 71 -> 8, handwheel 6 -> 3, plate force-0.5 datagen then force-0.6 revalidation 0 -> 0 | Blocking branch is likely definition/base-frame orientation mismatch causing bad grasp poses; active+mimic joint design is expected and no-linear-drive on `gripper_joint2` is not by itself a bug | Rotate/rebase Galaxea gripper USD or adjust wrapper/base frame so extraction yields the expected vertical approach convention (`approach_axis=2` if compatible), regenerate `.npz`, inspect YAML poses, then rerun helper validation before tuning grasp guess |
| `inspire_eg2_4c2` | `bots/inspire_eg2_4c2.usd` | Flattened pure candidate from `robot_usds/grippers/Inspire_EG2_4C2/`; collision APIs authored onto extracted asset; full per-prim physics audit still needed | Regenerated `bots/inspire_eg2_4c2.npz` on 2026-05-10 during helper validation; finite, `open_limit=upper`, row 0 width `0.0662`, closes to near `0.0`, `approach_axis=2`, `open_axis=0` | Full helper datagen generated provisional candidates for the current example set and plate | 2026-05-10 helper validation with headed `grasp_sim.py`: banana 842 -> 824, mug 677 -> 671, handwheel 690 -> 646, plate force-0.5 datagen then force-0.6 revalidation 816 -> 746 | Needs review: headed revalidation retained generated successes well, but final rates vs 1024 are below the 70% support threshold for mug 65.5% and handwheel 63.1%; banana 80.5% and plate 72.9% passed | Audit authored collisions/materials, then inspect low-success mug/handwheel poses for object-dependent grasp guess coverage, centering, bite depth, contact paths, and pose sampling before declaring support |
| `jodell_rg75` | `bots/jodell_rg75.usd` | Flattened pure candidate from `robot_usds/grippers/Jodell_RG75/`; repaired malformed `right_finger/finger_support` translate/scale ops that were authored as `None`; current USD has rigid base/fingers and prismatic finger joints, but finger collision meshes still author triangle mesh collisions that PhysX falls back to convex hull for dynamic bodies | Regenerated `bots/jodell_rg75.npz` on 2026-05-10 during helper validation; finite, `open_limit=upper`, `open_axis=1`, `approach_axis=2`, `finger_indices=[2,1]`, but `open_widths` are all negative (`-0.0238` to `-0.1005`) and the configured `bite=0.018` exceeds the finger collision approach-axis thickness (`0.0145`) | 2026-05-10 full helper datagen generated 0 candidates for banana, mug, handwheel, and plate; no datagen YAMLs were written | 2026-05-10 helper ran headed `grasp_sim.py`, but there were no generated grasps to revalidate; final counts are 0/1024 for all objects | Blocking branch is now definition/config geometry before sim validation: invalid open-width semantics and over-deep bite/finger collider selection prevent grasp guess/datagen from producing candidates | Reduce Jodell bite below actual pad depth, inspect/reselect finger collider geometry and bite point, regenerate `bots/jodell_rg75.npz`, verify widest pregrasp has positive opening and grasp closes toward the limit, then rerun a small datagen + headed sim smoke |
| `omnipicker` | `bots/omnipicker.usd` | Rechecked during 2026-05-11 validation: dynamic robot collision meshes still author triangle mesh collisions, and PhysX repeatedly falls back to convexHull for `gripper_base`, `narrow*_Link`, `wide*_Link`, and loop links; full per-prim physics audit still needed | Current `bots/omnipicker.npz` loads successfully; previous generated definition was finite with `open_limit=upper`, row 0 width `0.0776`, closing toward `0.0013` | Full datagen produced low object-dependent sim-filtered successes: banana 612, mug 223, handwheel 25, plate 305 out of 1024 | 2026-05-11 standard headed helper revalidation was unstable: 256-env and 64-env headed runs exited before writing sim YAML, while a 1-env/1-grasp headed smoke passed 1/1. Headless fallback `grasp_sim.py` revalidation completed: banana 612 -> 612, mug 223 -> 205, handwheel 25 -> 21, plate 305 -> 168 | Needs review: not supported at current thresholds. Final rates vs 1024 are banana 59.8%, mug 20.0%, handwheel 2.1%, plate 16.4%; plate also drops to 55.1% retained in revalidation. Blocking branches are object-dependent grasp guess/datagen coverage plus USD collision approximation/headed stability | Author proper convex/box/SDF collider approximations for dynamic OmniPicker bodies, audit USD physics/materials, then rerun headed revalidation at increasing env counts; tune grasp sampling/centering/bite for mug, handwheel, and plate before declaring support |
| `robotiq_85` | `bots/robotiq_85.usd` | Flattened pure candidate from `robot_usds/grippers/Robotiq_85/`; distinct from existing `robotiq_2f_85`; USD joint metadata has mimic attributes, but generated motion does not settle | Regenerated `bots/robotiq_85.npz` on 2026-05-09; finite, `open_limit=lower`, row 0 width `0.0886`, closes only to `0.0410`; convergence logging stayed near `0.02266` m max body delta for all 20 steps | Banana produced 512/512 candidates; mug produced 439 candidates in 3 tries | Staged smoke failed banana 0/16 and mug 0/16; all 32 grasps failed first-stage `normal_hold` | Blocking branch: USD drive/mimic solving or definition extraction settling. Evidence: driven `gripper_joint` samples only reach about `0.475` rad although the USD upper limit is `46.4` degrees, and the definition never closes below about 41 mm | Repair drive/mimic/definition-settling behavior or replace with the working `robotiq_2f_85` asset before more grasp tuning |

## Pure Gripper USD Candidates

These USDs look like pure or near-pure gripper assets but are not all registered or validated.

| Candidate | Path | Status | Next step |
| --- | --- | --- | --- |
| Piper H v1 | `bots/piper_h_v1_gripper.usd` | Registered as `piper_h_v1_gripper`; current `bots` USD has small symmetric finger collisions and existing non-plate datagen outputs are successful, but the original H source can reintroduce the oversized `Link8` collider if re-extracted blindly | Add extraction bbox sanity checks/overrides, regenerate the missing current-checkout `.npz` if needed, and rerun full validation |
| Piper L v1 | `bots/piper_l_v1_gripper.usd` | Registered as `piper_l_v1_gripper`; regenerated definition has valid stroke but bad `Link8` collision becomes the bite finger, giving a bad `bite_point` and very low validation (banana 12/1024, mug 3/1024, handwheel 43/1024) | Replace/crop the L `Link8` collision to a small finger-pad collider, regenerate `.npz`, then rerun the same validation probe |
| Piper v1 | `bots/piper_v1_gripper.usd` | Registered as `piper_v1_gripper`; no H/L-style oversized bite-finger collision in current `bots` USD. Non-plate validation is moderate/high on banana and mug but weak on handwheel; plate remains poor even at force 0.5 | Inspect failed plate/handwheel poses and tune guess centering/bite/offsets rather than treating it as a collision extraction bug |
| Piper X v1 | `bots/piper_x_v1_gripper.usd` | Registered as `piper_x_v1_gripper`; current USD has a latent oversized `Link8` collider, but generated definition uses the small `Link7` side as bite finger and validation is high (banana 953/1024, mug 953/1024, handwheel 788/1024, plate force-0.5 885/1024) | Keep current asset usable, but repair or sanity-check oversized `Link8` to prevent future extraction-order regressions |
| ChangingTek AG2F120S | `bots/changingtek_ag2f120s.usd` | Registered as `changingtek_ag2f120s`; 2026-05-10 full helper validation is `needs review`: banana 440/1024, mug 768/1024, handwheel 246/1024, plate 605/1024 after headed revalidation | Audit USD physics and diagnose object-dependent low datagen coverage on banana, handwheel, and plate |
| ChangingTek AG2F90 | `bots/changingtek_ag2f90.usd` | Registered as `changingtek_ag2f90`; small staged smoke has banana 8/16 and mug 3/16 final successes | Inspect mug failures and tune bite/centering |
| Galaxea G1 | `bots/galaxea_g1.usd` | Registered as `galaxea_g1`; 2026-05-10 helper validation failed: banana 0 -> 0, mug 71 -> 8, handwheel 6 -> 3, plate 0 -> 0; headed visualization shows the gripper lying sideways and `.npz` extracts `approach_axis=0` | Rebase/rotate the USD or config so the base frame approach convention matches the pipeline, regenerate `.npz`, then rerun full helper validation |
| Inspire EG2 4C2 | `bots/inspire_eg2_4c2.usd` | Registered as `inspire_eg2_4c2`; 2026-05-10 full helper validation is `needs review`: banana 824/1024, mug 671/1024, handwheel 646/1024, plate 746/1024 after headed revalidation | Review authored collisions/materials and tune mug/handwheel grasp guess coverage, centering, bite depth, and pose sampling |
| Jodell RG75 | `bots/jodell_rg75.usd` | Registered as `jodell_rg75`; 2026-05-10 helper validation failed before useful sim revalidation because datagen produced 0 candidates for all objects; regenerated definition has all-negative widths and `bite` larger than pad depth | Fix bite/collider/open-width definition semantics, regenerate `.npz`, then rerun small datagen and headed sim before tuning |
| OmniPicker | `bots/omnipicker.usd` | Registered as `omnipicker`; 2026-05-11 larger validation is `needs review`: headless fallback revalidation after headed batch crashes produced banana 612/1024, mug 205/1024, handwheel 21/1024, plate 168/1024; dynamic triangle mesh collisions repeatedly fall back to convexHull | Repair collider approximations/USD physics, diagnose headed batch instability, and tune object-dependent grasp generation for mug, handwheel, and plate |
| Robotiq 85 | `bots/robotiq_85.usd` | Registered as `robotiq_85`; small staged smoke is 0/16 on banana and mug despite provisional guesses | Compare against working `robotiq_2f_85` and fix close/joint semantics |

## Extraction Candidates

These sources may contain two-finger grippers but are full robots, arms, scenes, or mixed assets. They must not be used directly as final `gripper_file` values for data generation.

| Source | Path | Status | Next step |
| --- | --- | --- | --- |
| Local robot assets | `robot/` | Extraction candidates | Inspect each robot for two-finger end effectors; extract only the gripper into `bots/` |
| Humanoid robot USDs | `robot_usds/humannoid/` | Extraction candidates | Search for two-finger end-effector components and avoid full-body USDs |
| Manipulator USDs | `robot_usds/manipulators/` | Extraction candidates | Search end-effector attachments and extract only gripper geometry/physics |
| Piper Isaac Sim assets | `piper_isaac_sim/` | Extraction candidates | Use as reference for Piper variants when `bots/` USDs are incomplete |

## Grasp Simulation Roadmap

| Task | Status | Notes |
| --- | --- | --- |
| Stage 1: normal grasp attempt and hold | Implemented in `grasp_sim.py`; needs per-gripper acceptance review | Records `normal_hold` before disturbance forces begin |
| Stage 2: random disturbance-force hold validation | Implemented in `grasp_sim.py`; needs per-gripper acceptance review | Default tug directions are non-axis random-direction samples; YAML records per-attempt `disturbance_attempts` plus aggregate `disturbance_hold` |
| Stage 3: inverted gripper/object gravity hold | Implemented in `grasp_sim.py`; needs per-gripper acceptance review | Inverts the gripper/object pair, then applies object gravity as an equivalent downward force for `--inverted_gravity_duration` |
| Per-stage result metadata in YAML | Implemented | Top-level `validation` records stage settings; each grasp records stage booleans and first `failure_stage` |
| Stable revalidation of `grasp_sim` YAML output | Fixed | `grasp_file` reload now starts from saved `pregrasp_position`/`pregrasp_orientation` when using pregrasp c-space, and preserves existing pregrasp pose metadata when writing new sim output |
| Pregrasp-to-grasp close control | Fixed | `grasp_sim.py` now starts from saved pregrasp c-space, ramps toward the saved grasp `cspace_position` over `--initial_grasp_duration`, and stops advancing per-env targets once both finger contacts are detected instead of commanding the closed joint limit |
| Debug output for failed grasps | Partially implemented | Sim output can now retain failed `grasp_guess_buffer` grasps when `save_fails=True`; still add richer USD/contact diagnostics for root-cause classification |

## Failure Diagnosis Checklist

Use this checklist when a gripper cannot generate successful validated grasp data:

- Physical feasibility: can the gripper geometry, stroke, bite depth, and finger pads plausibly grasp the target object?
- USD physics: are mass, inertia, friction, collision APIs, collider approximation, rigid bodies, joint limits, and drives correct?
- Definition extraction: are `finger_colliders`, `base_frame`, `open_axis`, `approach_axis`, `open_limit`, `bite_point`, and generated c-space samples correct?
- Guess generation: is pregrasp the widest valid opening, is the grasp state closer to closed, are the object-relative transforms plausible, and are collision checks using the right bodies?
- Simulation validation: are contacts detected on the intended finger bodies, is the close direction correct, are force/gravity/object mass settings reasonable, and is the success criterion matching the intended behavior?
